import { FiltrarClientePipe } from './filtrar-cliente.pipe';

describe('FiltrarClientePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarClientePipe();
    expect(pipe).toBeTruthy();
  });
});
